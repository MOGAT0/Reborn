[gd_resource type="ShaderMaterial" load_steps=5 format=3 uid="uid://da4fpqwiehbet"]

[ext_resource type="Texture2D" uid="uid://cn2cyks4bpy0f" path="res://assets/3d objects/stylized_tree_0.png" id="1_ifiio"]

[sub_resource type="Shader" id="Shader_eryi3"]
code = "shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_disabled, diffuse_toon, specular_toon, depth_prepass_alpha;

uniform vec4 albedo : source_color;
uniform sampler2D texture_albedo : source_color, filter_linear_mipmap, repeat_enable;
uniform ivec2 albedo_texture_size;
uniform float point_size : hint_range(0.1, 128.0, 0.1);

uniform float roughness : hint_range(0.0, 1.0);
uniform sampler2D texture_metallic : hint_default_white, filter_linear_mipmap, repeat_enable;
uniform vec4 metallic_texture_channel;
uniform sampler2D texture_roughness : hint_roughness_r, filter_linear_mipmap, repeat_enable;

uniform float specular : hint_range(0.0, 1.0, 0.01);
uniform float metallic : hint_range(0.0, 1.0, 0.01);

uniform vec3 uv1_scale;
uniform vec3 uv1_offset;
uniform vec3 uv2_scale;
uniform vec3 uv2_offset;

/*********** WIND UNIFORMS ***********/
uniform sampler2D noise_tex;
uniform float wind_strength = 0.2;   // how much the leaf bends
uniform float wind_speed = 1.5;      // how fast the wind animates
uniform float tip_influence = 1.0;   // how much stronger the wind is at the leaf tip

void vertex() {
	// --- Your original UV transform ---
	UV = UV * uv1_scale.xy + uv1_offset.xy;

	// --- Wind animation ---
	float t = TIME * wind_speed;

	// Noise scrolling over time
	float n = texture(noise_tex, UV + vec2(t * 0.1, t * 0.05)).r;

	// The farther along +Y, the more the leaf bends (assumes leaf bottom is y=0)
	float bend_factor = clamp(VERTEX.y * tip_influence, 0.0, 1.0);

	// Waving sideways along X-axis (like a real leaf flutter)
	float wind_offset = (n - 0.5) * wind_strength * bend_factor;

	// Apply bending influence
	VERTEX.x += wind_offset;
	VERTEX.z += wind_offset * 0.3; // slight twist for realism
}

void fragment() {
	vec2 base_uv = UV;

	vec4 albedo_tex = texture(texture_albedo, base_uv);
	ALBEDO = albedo.rgb * albedo_tex.rgb;

	float metallic_tex = dot(texture(texture_metallic, base_uv), metallic_texture_channel);
	METALLIC = metallic_tex * metallic;
	SPECULAR = specular;

	vec4 roughness_texture_channel = vec4(1.0, 0.0, 0.0, 0.0);
	float roughness_tex = dot(texture(texture_roughness, base_uv), roughness_texture_channel);
	ROUGHNESS = roughness_tex * roughness;

	ALPHA *= albedo.a * albedo_tex.a;
}
"

[sub_resource type="FastNoiseLite" id="FastNoiseLite_ifiio"]
frequency = 0.0255
fractal_gain = 0.33
fractal_weighted_strength = 0.05

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_ifiio"]
noise = SubResource("FastNoiseLite_ifiio")
seamless = true

[resource]
resource_name = "leafes_Mat.001"
render_priority = 0
shader = SubResource("Shader_eryi3")
shader_parameter/albedo = Color(0.5197687, 1, 0.16459775, 1)
shader_parameter/texture_albedo = ExtResource("1_ifiio")
shader_parameter/albedo_texture_size = Vector2i(2048, 2048)
shader_parameter/point_size = 1.0
shader_parameter/roughness = 1.0
shader_parameter/metallic_texture_channel = Vector4(1, 0, 0, 0)
shader_parameter/specular = 0.5
shader_parameter/metallic = 0.0
shader_parameter/uv1_scale = Vector3(1, 1, 1)
shader_parameter/uv1_offset = Vector3(0, 0, 0)
shader_parameter/uv2_scale = Vector3(1, 1, 1)
shader_parameter/uv2_offset = Vector3(0, 0, 0)
shader_parameter/noise_tex = SubResource("NoiseTexture2D_ifiio")
shader_parameter/wind_strength = 1.0
shader_parameter/wind_speed = 1.5
shader_parameter/tip_influence = 2.0
